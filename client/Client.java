package client;

public class Client {
    public static void main(String[] args) {
        // Verifica args (IP e porta do server)
        // Tenta conexao 
        // Pede senha por teclado
        // autentica ou nao no server
        // Quando conectar, recebe IP do multicast (por enquanto, só printa multicast)
        // Ler teclado esperando msg
        // Enviar msg por tcp
    }

}